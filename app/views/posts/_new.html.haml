= form_for new, url: posts_path, method: :post, remote: true do |f|
  #form_block_textarea
    = f.text_area :content
  #form_block_submit
    = f.submit t('save'), "data-disable-with" => t('saving')
